import styled from 'styled-components'
import { COLORS } from './Colors'

export const TogglePillDiv = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 9999px;
  padding: 0.5rem;
  font-weight: 500;
  align-self: start;
  background-color: ${COLORS.lightGreen};
  color: ${COLORS.white};
  margin-left: 1rem;
`
